import styled from 'styled-components';
function Loading() {
  return <StyledLoading>로딩 중입니다 🫡</StyledLoading>;
}

export default Loading;

const StyledLoading = styled.li`
  font-weight: 700;
`;
